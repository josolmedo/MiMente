Lista los archivos que existen en el directorio actual.

```shell
ls -l: Muestra los archivos en formato largo
ls -a: Muestra todos los archivos, incluidos los ocultos
ls -lh: como -l pero el tamaño de los archive aparece en formato legible.
ls -lah: combinación de todos los demás.
```


[[pwd]]